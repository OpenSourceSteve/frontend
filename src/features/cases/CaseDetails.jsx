import styles from './cases.module.css'

export const CaseDetails = () => {
    return (
        <section className={styles.details}>
            <h1>This is the Case Details page.</h1>
        </section>
    )
}